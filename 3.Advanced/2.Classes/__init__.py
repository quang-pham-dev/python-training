# Class?
# A class is a blueprint for creating objects (instances) that have attributes (variables) and methods (functions).
# It acts as a template for creating objects with similar characteristics.
